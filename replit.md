# My Car Concierge - PWA & Native Apps

## Overview
My Car Concierge (MCC) is an automotive service marketplace PWA with native app support, connecting vehicle owners with service providers for booking, payments, and vehicle management. It aims to be a comprehensive solution for car ownership, focusing on security, user experience, service coordination, and smart shopping tools. The platform's vision is to become the complete auto ownership platform, offering a full-service experience for auto care.

## User Preferences
- **Brand Messaging**: "Your complete auto ownership platform"
- **Tone**: Professional, informative, memorable, and witty without being gimmicky
- **Key Headlines**: "One app. Every auto need. Zero hassle."
- **Four Pillars**: Get Quotes, Manage Vehicles, Maintaining Your Ride, Shop Smarter
- **Copy Terminology**: Use "auto" for general references (auto care, auto ownership), "ride" for casual/friendly tone (your ride, your next ride), "vehicle" for formal contexts (vehicle owners). Brand name "My Car Concierge" stays unchanged.
- **Automotive Theme**: Warmer dark slate backgrounds (#12161c) with bronze/copper gold accents and coolant teal highlights; premium, luxury garage-inspired aesthetic with metallic gradients on buttons and cards; less harsh dark mode with improved contrast and depth.
- **Light Mode**: Navy blue #1e3a5f for trust, deeper gold #b8942d for 23% higher CTA contrast, warmer off-white #fefdfb for approachability. WCAG-compliant text contrast with white button text on gold.
- **Theme Toggle**: Pill-shaped button with sun/moon icons and "Day"/"Night" text labels in header/navbar for clear accessibility.
- **Languages**: English, Spanish, French, Greek, Chinese, Hindi, Arabic (RTL supported).

## System Architecture
The application is built with a modern web stack (vanilla HTML, CSS, JS) and utilizes PWA capabilities for web, Capacitor for mobile, and Electron for desktop.

Key architectural patterns and features include:
- **PWA Features**: Installability, offline support, and auto-updates.
- **User Roles**: Supports `member`, `provider`, `pending_provider`, and `admin` roles, including dual roles.
- **Authentication**: Two-Factor Authentication (2FA), Sign in with Apple, and Magic Link.
- **Account & Team Management**: Self-service account deletion and provider team management with role-based access.
- **Service Coordination**: Appointment scheduling, vehicle transfer tracking, and temporary location sharing.
- **Provider Management**: Rating system, automated suspension, employee background checks, and compliance tracking.
- **Vehicle Tools**: "My Next Car" (prospective purchases with VIN lookup) and "Dream Car Finder" (AI-powered search).
- **AI Features**: OCR for insurance cards, AI explanations for OBD codes, AI bid analysis, AI Helpdesk, AI review summarization, AI fair price estimator, AI package builder, AI bid strategy insights, AI provider matching, and an AI Marketing Hub.
- **Notification System**: User-controlled push, email, and SMS notifications, including automated reminders.
- **E-commerce**: Integrated Merch Store.
- **Lead Capture**: Public survey page for prospect lead generation with analytics dashboard.
- **Referral & Commission System**: Founder referral program with lifetime commissions.
- **Payment System**: Care plan completion lifecycle with dispute endpoints, admin oversight, AI-assisted dispute resolution, and Stripe Connect escrow.
- **SaaS Billing Foundation**: Configurable plans, access control, and subscription management for various offerings including White-label Platform, Fleet SaaS, and Provider Shop SaaS.
- **Automotive AI API**: Developer API for VIN lookup, recalls, OBD codes, and price estimation.
- **Outreach Engine SaaS**: Plan-based lead limits for an autonomous outreach platform with lead discovery, scoring, and outreach pipeline.
- **Job Workflow**: Member QR check-in and provider confirmation.
- **Analytics**: Provider and Admin dashboards with traffic monitoring.
- **Security**: API Rate Limiting, Login Activity Log, server-side gating for privileged mutations, and admin audit logs.
- **Mobile Native Features**: Biometric Login, Mobile Wallet Payments, and FCM Push Notifications.
- **UI/UX Decisions**: Responsive design, hero sections, trust badges, signup progress indicators, onboarding checklists, specific color schemes, and theme toggle.
- **Performance Optimizations**: Lazy-loaded JS modules, preconnect, enhanced service worker caching, server-side pagination, and image lazy loading.
- **Linting (ESLint)**: Flat-config `eslint.config.js` at repo root with `eslint-plugin-unicorn` + `eslint-plugin-html` (all in `devDependencies`). Run `npm run lint` to check or `npm run lint:fix` to auto-fix. Only mechanical, auto-fixable rules are enabled (`unicorn/prefer-number-properties`, `unicorn/prefer-global-this`, `unicorn/prefer-string-replace-all`, `unicorn/prefer-class-fields`, `prefer-object-has-own`, `no-unused-vars`, `no-negated-condition`) so unrelated SonarCloud-style noise does not surface. All seven are configured at `warn` severity (auto-fixers still apply on warnings) so `npm run lint` exits 0; the leave-alone warnings are findings the fixer can't safely resolve (e.g. `unicorn/prefer-number-properties` on `isNaN()` calls where the arg isn't provably numeric). Nested-ternary, Cognitive Complexity, accessibility, and security/SRI findings are intentionally left out — they need human refactors. Ignored paths: `node_modules`, `www/`, `android/`, `ios/`, `dist/`, `.netlify/`, `.netlify-deploy/`, `.cache/`, `attached_assets/`, `test-results/`, `playwright-report/`, `rideshare-calculator/` and `outreach-runner/` (both ESM subprojects with their own builds), `electron/`, `supabase/`, `tools/`, `netlify/`, `.local/`, `.playwright/`, and `**/*.min.js`. After running `lint:fix`, run `npm run cap:sync` so the Android-bundled HTML copies pick up the changes.
- **AI Ops Agent Fleet**: Multi-agent system (Orchestrator, Analyst, Matchmaker, Treasurer, Gatekeeper, Concierge, Advocate, Hunter, Director, Promoter) for autonomous administration, social acquisition, and dispute resolution. Includes editable prompt versioning with rollback and a side-by-side diff viewer.
- **Conversational Onboarding**: Progressive profiling signup flows for members and providers.
- **Facebook Pixel & Conversions API (Marketing Analytics)**: Funnel-event tracking that lets ad-spend ROI be measured in Facebook Events Manager. Single shared loader `fb-pixel.js` (root + `www/`) installs the standard Pixel base code and exposes `mccTrackFb`, `mccTrackFbOnce`, and `mccTrackFbPurchase` helpers. Pixel ID is read at runtime from `globalThis.MCC_FB_PIXEL_ID` or `<meta name="mcc-fb-pixel" content="...">`; if neither is set the helpers degrade to no-ops so dev/staging never accidentally fires test events into production. Loaded on every public page (`index.html`, `login.html`, signup pages, `survey.html`, marketing pages under `www/marketing/`, `about.html`, `how-it-works.html`, `contact.html`, `faq.html`, `fleet-landing.html`, `founders.html`, plus the post-Stripe landing pages `members.html`, `providers.html`, `car-club-member.html`). Standard events: `PageView` (auto), `Lead` (survey profile submit, signup-page entry via `<meta name="mcc-fb-event">`), `CompleteRegistration` (member onboarding submit, both `onboarding-member.html` copies + `signup-member.html`), `Subscribe`/`Purchase` (auto-detected on `?checkout=success` and `?purchase=success` landing URLs so existing redirect URLs need no changes). Server-side mirror via Conversions API: `netlify/functions/facebook-conversions-api.js` mounted at `POST /api/fb/conversions`; SHA-256 hashes user-data PII per Facebook spec, picks up `client_ip_address`/`client_user_agent` from request headers, and supports `event_id` for browser+server dedup. Requires `FACEBOOK_PIXEL_ID` and `FACEBOOK_CAPI_TOKEN` env vars on Netlify (optional `FACEBOOK_TEST_EVENT_CODE` for the Events Manager Test Events tab). Coexists with the existing Facebook JavaScript SDK on `index.html` / `www/index.html` (Task #182, separate `sdk.js`/`fbevents.js` scripts and separate `FB`/`fbq` globals).
- **Facebook Sign-In (Members)**: "Continue with Facebook" button on the public sign-in (`login.html`, `www/login.html`) and member sign-up (`signup-member.html`) flows. Uses Supabase Auth's Facebook OAuth provider (`supabaseClient.auth.signInWithOAuth({provider:'facebook'})` with scopes `email,public_profile`); requires the Facebook provider enabled in the Supabase Auth dashboard with App ID `1210351411006961`. After the redirect lands back, the page detects `?oauth=facebook` and creates a `member` profile row if none exists; brand-new Facebook signups are forwarded to `onboarding-member.html?source=facebook` so they complete the same conversational survey email signups do (the survey skips name/email/password steps and pre-fills the name from `user.user_metadata`, then `submitSignup` updates rather than inserts the existing profile row). Existing accounts that share the Facebook email are linked by Supabase via the verified-email provider link, so duplicates aren't created. The OAuth flow does a full redirect to facebook.com, so no client-side FB SDK is required on the auth pages (the SDK injected on `index.html` from Task #182 stays for marketing/share features).
- **Service Categories**: Property-based service categories including Snow Removal Services.
- **Car Club Loyalty System**: Per-provider loyalty clubs with punch card rewards.
- **Smart Service Recommendations**: AI-driven service suggestions based on vehicle make/model.
- **AI Helpdesk Widget**: Anthropic Claude-powered chat with Car Expert, Provider Support, and Car Academy modes.
- **Blog (`/blog/`)**: SEO-driven editorial pillar with evergreen posts, static HTML generation, JSON-LD, SEO meta injection, and sitemap generation.
- **Provider Onboarding Walkthrough**: 7-step interactive guided tour for new providers.
- **Facebook Page Connection (Admin)**: OAuth-driven picker in the admin Marketing → Outreach panel that satisfies Meta App Review for the `pages_show_list` permission. Server endpoints under `/api/admin/facebook/*` (oauth-start / oauth-callback / pending-pages / select-page / disconnect / connection); HMAC-signed one-time `state` param protects the callback; persisted Page ID + name only (no access tokens) in the singleton `facebook_page_connections` Supabase table. Dev: `www/server.js` (in-memory caches). Prod: `netlify/functions/admin-facebook.js` (stateless — pending /me/accounts result is held in a short-lived HMAC-signed httpOnly Secure `mcc_fb_pending_pages` cookie between callback and selection); `_redirects` routes `/api/admin/facebook/*` → `/.netlify/functions/admin-facebook`. Admin auth on prod is `x-admin-password` matching `ADMIN_PASSWORD`. UI renderer (`renderFacebookPageCard` / `initFacebookPageConnection` in `www/admin-outreach.js`) populates `#facebook-page-connection-card` whenever the Marketing & Outreach panel becomes visible (auto-init from the panel's setup) and handles `?picking=facebook-page` and `?fb_error=` callback params via `fbAutoSwitchOnCallbackReturn`. The select-page POST sends only `page_id`; the Netlify function authoritatively reads `page_name` from the signed pending-pages cookie so the client can't spoof it. Requires `FACEBOOK_APP_SECRET` env var on prod.
- **Concierge Driver Coordination (Task #369)**: Member-initiated and provider-initiated concierge driver requests reusing the authoritative scenario→legs expansion from Task #332. Shared module `netlify/functions/_concierge-scenarios.js` exports `EXPAND_SCENARIO`, `SCENARIO_TIER`, and `expandLegs` so admin (`concierge-jobs-admin.js`) and public (`concierge-jobs-public.js`) functions can never drift. Public function is mounted at `/api/concierge/*` (Netlify) and dev-mirrored in `www/server.js`; auth is the user's own Supabase JWT (`Authorization: Bearer …`) verified via `supabase.auth.getUser(token)`. Routes: `GET /api/concierge?role=member|provider`, `GET /api/concierge/:id`, `POST /api/concierge` (members supply `member_id` implicitly; providers must reference an appointment they own; `member_id` / `provider_id` are read authoritatively from `appointments`, never trusted from the body), `POST /api/concierge/:id/cancel`, `POST /api/concierge/:id/transition` (per-role allow-list `TRANSITIONS`: provider can drive `scheduled→vehicle_received→vehicle_released→completed`; either party can `problem_flagged`). Migration `supabase/migrations/20260515d_concierge_created_by_kind.sql` adds `created_by_kind` (`admin|member|provider|system`) + `created_by_id` columns + indexes, AND extends the `concierge_jobs.status` CHECK constraint to include `requested`, `vehicle_received`, `vehicle_released`, `problem_flagged`. Audit rows go to `admin_audit_log` (`create_concierge_job` / `cancel_concierge_job` / `transition_concierge_job`, `performed_by = auth.uid()`, `metadata.source`); agent events emit `concierge.job_requested`, `concierge.job_cancelled`, `concierge.status_changed`. Member UI: "Request a Driver" button + scenario-picker modal under the appointment card in `www/members-extras.js` (`renderAppointmentStatus`) with auto-filled pickup (member's saved address) and dropoff (provider's shop address), preferred-pickup-time field, and post-submit success state showing the job reference. Inline status badge polls `GET /api/concierge?role=member` and lets the member cancel their own request. Provider UI: "MCC Driver Concierge" panel inside each job dashboard in `www/providers.js` (`renderJobDashboard` → `renderConciergeSection`), showing inbound requests with an accepted-driver count, transition action buttons (`Mark Received`, `Mark Released`, `Mark Completed`, `Flag Problem`) wired to `window.transitionConciergeJob`, plus a "Request Driver For This Job" modal that sends `created_by_kind:'provider'`. Auto-refresh uses a per-card `__conciergeJobMap` (packageId → appointmentId) so the 30s interval preserves appointment scoping. Smoke tests: `node netlify/functions-tests/concierge-public.test.js` (16 tests covering missing-bearer 401, ownership checks, provider role gating, cancel rules, scenario-table parity, role-scoped transitions, disallowed-hop 409s, non-party 403s, vehicle-ownership 403, provider shop-address edit pre/post driver acceptance, member-edit 403, and the round-11 provider-mode/no-appointment 400 contract). Full member/provider API contract appended to `docs/driver-app-api.md`.
- **Driver App Backend (Task #332)**: Server-side foundation for the separate "MCC Driver" Replit project. Schema in `supabase/migrations/20260514c_driver_concierge_jobs.sql` (drivers, concierge_jobs, concierge_job_legs, concierge_job_drivers, driver_location_pings, driver_earnings) with read-only RLS for drivers and service-role-only writes. Twilio Verify phone-OTP auth in `netlify/functions/driver-api.js` (mounted at `/api/driver/v1/*` via `www/_redirects`); after Twilio confirms the code, the server mints a **native Supabase session** via `auth.admin.generateLink({type:'magiclink', email})` + anon-client `auth.verifyOtp({token_hash})`, so the Driver app receives real Supabase access/refresh tokens and can talk to Supabase directly under the existing RLS policies (`auth.uid() = drivers.profile_id`). Per-phone OTP send-rate limit (3/15min) is backed by the `driver_otp_send_log` table so it's shared across Netlify instances and survives cold starts. Admin job creation/assignment under `/api/admin/concierge-jobs/*` (`netlify/functions/concierge-jobs-admin.js`) with `EXPAND_SCENARIO` table that authoritatively expands the 4 tiers / 11 scenarios into legs (clients never invent legs); writes `admin_audit_log` rows (`create_concierge_job`, `assign_concierge_driver`, `cancel_concierge_job`) and emits `agent_events`. Dev mirror in `www/server.js` adapts Netlify handler signatures for both endpoints. Full API contract in `docs/driver-app-api.md`. Smoke tests: `node netlify/functions-tests/driver-api.test.js`. Required env vars (prod): `TWILIO_ACCOUNT_SID`, `TWILIO_AUTH_TOKEN`, `TWILIO_VERIFY_SERVICE_SID`, plus the existing `SUPABASE_URL` / `SUPABASE_SERVICE_ROLE_KEY` / `SUPABASE_ANON_KEY` / `ADMIN_PASSWORD`.
- **Stripe Key Expiry Reminder (Task #246)**: The Stripe secret key currently expires 7 days after it's set, so the platform tracks a configurable expiry date in `ai_ops_settings` (key=`stripe_key_expiry_date`, value `YYYY-MM-DD`). A daily Netlify scheduled function (`netlify/functions/stripe-key-expiry-scheduled.js`, cron `0 12 * * *`) compares "now" to the stored date and emails the admin (`jm.zanetis@gmail.com` via Resend) at the **3-day**, **1-day**, and **0-day / expired** thresholds. Each run logs to `ai_action_log` (module=`stripe_key_expiry`, action_type=`alert_3d`/`alert_1d`/`alert_expired`/`check`); the alreadyAlerted check filters by `created_at >= ai_ops_settings.updated_at`, so the same threshold is never double-sent within a cycle, and updating the date in admin advances `updated_at` which automatically resets the alert state. The admin Payments section shows a colored pill (green `>3d` / yellow `≤3d` / red `≤1d` or expired) plus the date and an inline date input + "Update Expiry" button. **Rotation procedure**: (1) rotate the key in Stripe, (2) update `STRIPE_SECRET_KEY` in Netlify env vars, (3) open admin → Payments and set the new expiry date so the alert ladder resets. Admin endpoints `/api/admin/stripe-key-expiry` (GET status / POST `{expiry_date}`) and `/api/admin/stripe-key-expiry/run-now` are mounted via `www/_redirects` → `netlify/functions/stripe-key-expiry-admin.js` in prod and mirrored in `www/server.js` for dev.
- **BackgroundChecks.com Live Integration (Task #372)**: Real BGC private + public API wiring for provider voluntary employee background checks, gated behind the `BGC_LIVE_MODE` env flag (default off → existing mock path stays). Order endpoint `POST {BGC_API_BASE}/orders/new?api_token=<key>` is called from `netlify/functions/initiate-background-check.js` with `{report_sku:'HIRE1', order_quantity:1, applicant_emails:[employee.email], terms_agree:'Y'}` — **SSN/DOB never touch MCC**; BGC collects PII directly from the applicant via the `applicant_invite_url` returned in the response (or via the embedded JS widget). The provider's own sub-account API key (from `provider_background_check_accounts.bgchecks_api_key`) is preferred; falls back to platform-wide `BGC_API_TOKEN`. Webhook (`background-check-webhook.js`) handles BGC's letter-code statuses (`A`=awaiting → pending, `P`=pending → pending, `C`=complete → `clear` or `consider` when `flagged_for_end_user_review:true`); HMAC-SHA256 signature verification against `BGC_WEBHOOK_SECRET` accepts both `<hex>` and `sha256=<hex>` formats. **Sub-account onboarding**: provider visits `/bgc-enroll-account.html` → BGC `ClearChecksWidget` registration form → encrypted token POSTed to `/api/provider/bgc/decrypt-token` → server calls `POST /token/decrypt` with `BGC_PRIVATE_KEY` → upserts `provider_background_check_accounts` with `bgchecks_api_key` + `live_mode=true`. Decrypted API key is never returned to the browser. **Admin visibility**: `GET /api/admin/bgc/providers` (auth: `x-admin-password` OR Supabase admin JWT) returns one row per provider with `live_mode`, `mode_reason`, `pending_count`, `completed_count`, and `bgchecks_account_id`. **Required env vars (prod)**: `BGC_LIVE_MODE='true'`, `BGC_API_BASE` (default `https://app.backgroundchecks.com/api`; sandbox `https://sandbox.backgroundchecks.com/api`), `BGC_API_TOKEN` (platform fallback), `BGC_PRIVATE_KEY` (RSA for `/token/decrypt`), `BGC_SOURCE_TOKEN` (registration widget), `BGC_PUBLIC_FORM_BASE` (optional, defaults to API host), `BGC_WEBHOOK_SECRET` (existing). Migration `supabase/migrations/20260515e_bgc_live_mode.sql` adds `employee_background_checks.applicant_invite_url` plus `provider_background_check_accounts.bgchecks_api_key` / `live_mode` / `source_token`, relaxes `bgchecks_account_id` NOT NULL, adds `chk_bgc_account_has_credential` check (account_id OR api_key required), **revokes column-level SELECT on `bgchecks_api_key` from `authenticated`/`anon`** so the existing per-provider RLS row policy can't expose the decrypted secret to browser clients (only `service_role` reads it), and creates the `provider_background_check_accounts_public` view (no api_key column, exposes a `has_api_key` boolean) for the provider dashboard's "is your account linked?" check. Routes added to `www/_redirects`: `/api/provider/bgc/config` → `bgc-config`, `/api/provider/bgc/decrypt-token` → `bgc-decrypt-token`, `/api/admin/bgc/providers` → `bgc-admin`. Smoke test: `node netlify/functions-tests/bgc-live.test.js` (15 tests; stubs all BGC HTTP via `global.fetch`, no live creds required).
- **Deployment Architecture**: GitHub for source control, Netlify for production hosting, Replit for development.

## External Dependencies
- **Supabase**: Backend as a Service (PostgreSQL, authentication, storage).
- **Stripe**: Payment processing (Stripe Connect).
- **Capacitor**: Cross-platform native runtime.
- **Electron**: Desktop application framework.
- **Twilio**: SMS services.
- **Netlify**: Deployment and hosting.
- **OpenAI**: AI integration.
- **Anthropic**: AI (fallback for Dream Car Finder, primary for AI Helpdesk & Marketing Hub, agent fleet).
- **Google Gemini**: Primary AI (Dream Car Finder & Research & Outreach Agent).
- **Resend**: Email delivery services.
- **Google Cloud Vision**: OCR for document verification.
- **HubSpot**: CRM integration.
- **Google Places API**: Used by AI Outreach Engine.
- **Instantly.ai**: Cold email outreach platform.
- **BackgroundChecks.com**: Background screening integration.