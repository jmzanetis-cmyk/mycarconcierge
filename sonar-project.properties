sonar.python.version=3.11

# www/**/*.html are multi-page app templates with shared nav/header/footer boilerplate.
# JS logic lives in separate .js files; inline scripts in HTML are pre-existing and
# tracked separately. Exclude HTML from all Sonar analysis to avoid false gates on
# structural duplication and pre-existing inline-script findings.
sonar.exclusions=www/**/*.html
